---
name: agentlab-experiment-learning
description: Turn captured experiments into evidence-linked lessons, executable regressions and explicit reusable knowledge candidates.
metadata:
  agentlab-role: maintenance
  agentlab-layer: method
  agentlab-stage: experiment-learning
---

# Experiment learning and publication

Consume fixed evaluation-instance and knowledge cuts. Use generic TableGit
programming analysis; retain its exact code/request, input cut and typed result
in analysis_records. Keep raw producer evidence complete. A failed Agent action
is a valid observation, not automatically a Harness defect or a proven cause.

Maintain experiment_lessons, lesson_evidence and lesson_validations in the
experiment repository. Each lesson records phenomenon, scope, attribution,
cause/change when known, source identities, evidence, validation and target IDs.
Observations remain observed; tested lessons become verified or rejected.
The first executable recipe verifies loading lifecycle cancellation with the
pinned unmodified source, operator scheduler, reference and omitted-cancellation
negative variant. Its scope is an isolated seam, not full Harmony/UI acceptance.

Promote a lesson explicitly by ID after its positive/negative validation. Read
the committed lesson export first. Export a candidate for the three reusable
knowledge tables: a structured verified-lesson program fact and a scope-specific
method Skill row, with source repository/cut/table prefix, digests and lineage.
The target-specific codebase Skills are a separate layer and should be updated
when experiments establish a contract that applies to that codebase.

An Action must not silently overwrite active knowledge. It publishes a separate
candidate export for review and maintenance import. Maintain knowledge in its
own TableGit destination, export a committed sorted cut, then commit that snapshot
to Release. Verified experience is not a license to change frozen task scoring.

Solidify actionable fixes in code and executable negative regressions, not only
prose. Distinguish Harness, environment, benchmark and participant problems.
Add a method lesson only when evidence supports its stated applicability;
retain contradictory and failed validation in instance history.

Run [the executable experience demo](../../examples/knowledge-seed/experience/README.md).
Public main knowledge Action executes calibration, instance import, generic SQL,
lesson persistence/repeat, committed export and explicit promotion candidate.
It independently proves the published active knowledge is unchanged. Always
upload failures, original calibration outputs and candidate/source-cut receipts.
Archive evidence needed for durable lesson references as an independent evidence
Release; GitHub Artifact is convenient access, not the only preservation layer.

Real Agent experiments consume the same modeling/promotion recipe but require
their own evidence and qualification. Do not rename this synthetic observation
demo a real Agent run, an automatic task generator, or a completed flywheel.

For a new maintainer: read goal, semantic/program-analysis and seed/calibration
Skills, reproduce the fixed experiment, inspect its lesson evidence, update the
appropriate method versus target layer, verify regressions, and leave HANDOFF.


### Scenario-owned calibration contracts

`agentlab-experience contract loading|debounce|image-url` emits the owned lesson
identity, variant expectations, scope, target IDs and reusable guidance. Freeze
this contract with the independently executed calibration in the runtime instance.
Observe every declared baseline/reference/negative outcome, archive generic
failed-check and tool-error SQL, and promote the explicit committed lesson.
Promotion derives its fact/Skill identity and qualification from that durable
contract; never reuse loading conclusions for click or image evidence. Source
Agent failures remain observations, separate from verified calibration-method
lessons. Frozen operational knowledge must remain unchanged after promotion.
Replay raw Release archives or original subject artifacts without redispatching
Agent/build; preserve original capture identity and HAP URIs. Temporary mutation
source trees stay outside exported calibration receipts and reusable JSONLs.


A fresh runner can encounter a connection reset while downloading an already
published dependency. Retain the failed acquisition receipt; retry transport
errors in the shared downloader rather than changing locks or treating the
network failure as an assessed Agent failure. Keep dependency identities fixed.


Keep calibration manifests analytical: variant verdict/checks plus exact raw
receipt path, capture identity, SHA256 and bytes. Full oracle output, submitted
methods and AST facts remain complete in runtime evidence/source tables. Do not
embed every variant's whole source/AST into one lesson-evidence row: that duplicates
observations and can exceed the TableGit transport frame. No bytes are removed;
the small manifest references the independently preserved complete raw receipt.
